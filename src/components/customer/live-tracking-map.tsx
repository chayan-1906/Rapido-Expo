import {Text, View} from "react-native";

function LiveTrackingMap({height, status, drop, pickup, captain}) {
    return (
        <View>
            <Text>Live Tracking Map</Text>
        </View>
    );
}

export default LiveTrackingMap;
